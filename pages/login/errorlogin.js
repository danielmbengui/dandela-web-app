import React from 'react';
import ErrorLogin from '../../components/Index/Login/ErrorLogin';

export default function ErrorLoginPage({logo, user}) {
  return (
    <ErrorLogin logo={logo} user={user} />
  )
}